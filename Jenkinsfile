pipeline {
    agent any

    stages {
        stage('Test') {
           
        }

        stage('E2E') {
           
        }
    }

    post {
        always {
            junit 'jest-results/junit.xml'
        }
    }
}
